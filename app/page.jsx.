import { ClerkProvider } from '@clerk/nextjs'
import './globals.css' // Yeh line add kar dena design ke liye

export default function RootLayout({ children }) {
  return (
    <ClerkProvider>
      <html lang="en">
        <body>{children}</body>
      </html>
    </ClerkProvider>
  )
}
